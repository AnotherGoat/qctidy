[package]
name = "benchmarks"
version.workspace = true
edition.workspace = true

[dependencies]
qctidy = { path = "../crates/library" }
criterion = { workspace = true }

[[bench]]
name = "performance"
harness = false
