[package]
name = "benchmarks"
version.workspace = true
edition.workspace = true

[dependencies]
qsimplify = { path = "../crates/library" }
criterion = { workspace = true }

[[bench]]
name = "performance"
harness = false
