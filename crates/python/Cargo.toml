[package]
name = "qsimplify-python"
version = "0.1.0"
edition = "2024"

[dependencies]
qsimplify = { path = "../simplifier" }
qsimplify-io = { path = "../io" }
pyo3 = { version = "0.28", features = ["extension-module"] }
